/* Resolve the driver's absolute-style include to the same repository header
 * as its relative includes, rather than a second copy in a build tree. */
#include "../../../../../libfprint-driver/goodix5xx.h"
